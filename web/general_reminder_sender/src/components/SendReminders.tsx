type SendRemindersProps = {
    prevStage: () => void,
    nextStage: () => void
}

export default function SendReminders({prevStage, nextStage}: SendRemindersProps) {
    return <div>SendReminders</div>
}
